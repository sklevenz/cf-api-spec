post:
  summary: Upload buildpack bits
  description: >-
    Upload a zip file containing a Cloud Foundry compatible buildpack. The file
    must be sent as part of a multi-part form.
  operationId: uploadBuildpack
  tags:
    - Buildpacks
  parameters:
    - $ref: ../components/parameters/Guid.yaml
  requestBody:
    description: The buildpack file to be uploaded
    required: true
    content:
      multipart/form-data:
        schema:
          type: object
          properties:
            bits:
              type: string
              format: binary
            resources:
              type: array
              items:
                type: object
                properties:
                  path:
                    type: string
                  size_in_bytes:
                    type: integer
                  checksum:
                    type: object
                    properties:
                      value:
                        type: string
  responses:
    '200':
      description: Successfully uploaded buildpack
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Buildpack.yaml
    '202':
      description: Upload initiated
      headers:
        Location:
          schema:
            type: string
          description: URL of the job tracking the upload
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Buildpack.yaml
    '401':
      $ref: ../components/responses/401.yaml
    '403':
      $ref: ../components/responses/403.yaml
    '404':
      $ref: ../components/responses/404.yaml
    '422':
      $ref: ../components/responses/422.yaml
