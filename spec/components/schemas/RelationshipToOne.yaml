type: object
properties:
  data:
    oneOf:
      - $ref: ./Relationship.yaml
      - type: 'null'
description: >
  Some relationships relate a resource to exactly one other resource. For
  example an app can belong to only one space.